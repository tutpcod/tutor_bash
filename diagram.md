```mermaid
graph TD
    A[Начало] --> B[Получить список подпапок в 'DATE'];
    B --> C[Записать абсолютные пути подпапок в массив A];
    C --> D{Цикл по элементам A<br/>i = 0 to длина(A) - 1};
    
    D -- Есть следующий A[i] --> E[Текущая папка = A[i]];
    E --> F[Получить список файлов в текущей папке];
    F --> G[Записать пути файлов в массив V];
    G --> H{Цикл по элементам V<br/>j = 0 to длина(V) - 1};
    
    H -- Есть следующий V[j] --> I[Текущий файл = V[j]<br/>(Начать обработку)];
    I --> J[Обработать файл];
    J --> H;
    
    H -- Все файлы обработаны --> K{Конец цикла по V};
    K --> D;
    
    D -- Все папки обработаны --> L[Конец];
